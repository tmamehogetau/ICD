Original prompt: このゲームをブラウザゲームとして実装したい

## 2026-07-11

- 実装方針: 依存追加なしのHTML/CSS/ES Modules。ローカル1台のホットシート方式。
- 端末受け渡し画面を、手札確認・投票の前に必ず挟む。
- 調整カードの適用可否は自動判定せず、卓上合意と完成カードの手入力に委ねる。
- 仕様書とベースカード一覧は変更しない。

## 実装済み

- 全54ベースカード（全件フォロワー、空モード保持）を構造化した。
- 調整カード40枚、ホットシート進行、秘密投票、得点、決選投票を実装した。
- 紙の企画書・赤入れ・承認スタンプを軸にしたレスポンシブUIを実装した。
- `window.render_game_to_text()`、`window.advanceTime(ms)`、Fキーの全画面切替を追加した。
- 企画見直しの交換は、各プレイヤー各ラウンド1回・最大2枚に制限した。

## 検証

- `npm.cmd test`: 9/9成功。
- アプリ内ブラウザで3人・3ラウンドを開始から決選投票、最終順位まで完走。
- 開始、制作、投票、最終結果を目視確認。
- ブラウザのconsole error/warnは0件。
- 6人プレイ時も使用済み調整カードが同一ラウンド中に再配布されないことをテストした。
- ナーフ理由の必須化・結果表示、0枚交換の拒否、交換時の入力保持、交換後の操作無効化、画面遷移後のフォーカスを確認した。
- 専用PlaywrightクライアントはプロジェクトにPlaywright依存がないため起動不可。依存を追加せずアプリ内ブラウザで代替した。

## 今後の候補

- オンライン同期、ルーム作成、途中保存はMVP外。
- 決選投票の同点時は、暫定で同率候補すべてを看板カードとして各作成者へボーナスを付与する。
## 2026-07-11 カードデータ分離

- `data/base-cards.json` をベースカード54枚の管理用正本にした。
- `data/adjustment-cards.json` に調整カード40枚を分離した（現在はテキスト正本からの生成物）。
- `src/cards.js` と `src/adjustments.js` は既存参照を維持するJSON読込ラッパーへ変更した。
- 固定ID、必須項目、値の型、名称重複、クラス、カテゴリ配分をテストへ追加した。
- 検証成功後だけ一時ファイルからJSONを置き換える。
- `npm.cmd test`: 10/10成功。実ブラウザでゲーム開始とカード表示、console error/warn 0件を確認した。
## 2026-07-11 調整カードのテキスト管理

- `data/adjustment-cards.txt` を調整カードの編集用正本にした。
- 各カードは空行区切りの `id/category/name/text` 形式で、コメント行も利用できる。
- `scripts/build-adjustment-cards.mjs` で必須項目、空欄、40枚以上、不正ID、ID・名前重複、カテゴリを検証し、`data/adjustment-cards.json` を生成する。
- `npm.cmd run cards:build` を追加し、`npm.cmd start` と `npm.cmd test` の前にも自動生成する。
- 検証成功後だけ一時ファイルからJSONを置き換える。
- 検証成功後だけ一時ファイルからJSONを置き換える。
- `npm.cmd test`: 10/10成功。


## 2026-07-11 調整カードID再採番

- 編集後の調整カード121枚を、現在の並び順で `a1` から `a121` へ連番化した。
- 同名カード5組は内容を変更せず許可し、ゲーム上はIDで区別する。
- 空行なしでも次の `id:` を新カードとして認識し、`text` 後の注記行は継続テキストとして保持する。
- JSON再生成成功、`npm.cmd test` は10/10成功。
## 2026-07-11 ベーシック調整カード対応

- 調整カードを現行順で通常 `a1`〜`a126`、ベーシック `b1`〜`b9` に再採番した。
- カテゴリを `数値 / 効果 / 条件` の3種へ統一し、生成時に a/b 別の連番、枚数、名前重複を検証する。
- 山札・手札・交換・捨て札は通常カードだけを対象とし、ベーシックカードは手札外の共通枠から枚数制限なく使用できる。
- 完成案には通常カードとベーシックカードのIDをまとめて保存する。
- `npm.cmd test`: 11/11成功。

## 2026-07-11 調整カード監査

- 記号不整合3件（潜伏、威圧、操り人形）を修正した。
- 編集ファイルの旧カテゴリ/IDを再同期し、数値14・効果107・条件13、通常a1〜a125、ベーシックb1〜b9へ復元した。
- 同名の「リアニメイト」は a60 を「リアニメイト進化」へ改名した。
- 発動条件不足はルール違反にせず、プレイヤー評価へ委ねる。
- 不明瞭テキストと近似/上下互換候補はユーザー確認事項として抽出した。
- 括弧機械検査は問題0件、`npm.cmd test` は12/12成功。
## 2026-07-11 数値カードの複製

- 調整カード定義に任意の `copies` を追加し、省略時は1枚としてJSONへ数値で出力するようにした。
- 数値カード `a1`〜`a12` は各2枚、`a13`（数値半減）と`a14`（倍プッシュ）は各1枚。数値カードは合計26枚。
- 山札・手札・捨て札・使用済み管理は `a1#1` のような物理実体IDを使用し、表示と完成案では定義カードへ解決する。
- 同じ数値カードの別コピーは、同一の完成案に同時使用できる。
- 通常の物理山札は137枚。`npm.cmd test` は12/12成功、実ブラウザでも制作画面とベーシック表示を確認した。

## 2026-07-11 オンライン正式版の土台

- ホットシート方式を置き換え、ルーム作成・招待URL参加・同端末復帰を実装した。
- Nodeサーバーをゲーム状態の唯一の正本とし、Bearerトークンで操作者を識別する。手札・未公開票は閲覧者ごとの状態投影で秘匿する。
- 既存の制作・秘密投票・決選投票を、各自の端末から同時に提出できるようにした。
- `data/online-rooms.json` に一時ファイル経由で保存し、サーバー再起動後も会議を復元する。
- `tests/online-game.test.js` を追加。通常・オンライン合わせて `npm.cmd test` は16/16成功。
- HTTPで作成→3人参加→開始→制作順開始を確認。ブラウザで会議室作成・招待待機室を目視確認し、console error/warnは0件。

## 次の候補

- 公開環境へのデプロイ（HTTPSと永続ストレージ）
- ルーム管理画面、観戦者、途中退出とホスト移譲
- 長期運用向けの共有DBと監査ログ


## 2026-07-11 同時進行とDCGカード配置

- オンラインの制作・通常投票・決選投票を、全プレイヤーが同時に提出できる方式へ変更した。全員の提出がそろった時だけ次の工程・集計へ進む。
- 同時提出中の `exchange` / 提出アクションは古い画面バージョンでもサーバー側検証のうえ受理し、順番待ち由来の競合を避ける。
- 原案・制作中の縮小原案・完成案・結果・決選候補を含む全カードで、攻撃を左下、体力を右下の宝石表示に統一した。
- `npm.cmd test`: 16/16成功。ブラウザで縮小原案の下角ステータス表示、console error/warn 0件を確認。

## 2026-07-12 提出状態と取消

- 完成案の提出後は、提出済みスタンプ・提出数・自分の完成カードを表示する画面へ遷移する。
- 全員の提出がそろう前だけ、提出を取り消して調整フェーズへ戻れる。通常調整カードを使用していた場合は手札へ戻す。
- `npm.cmd test`: 17/17成功。ブラウザで提出済み画面と取消→調整画面への復帰、console error/warn 0件を確認。

## 2026-07-12 評価画面

- 社内評価・決選投票とも、公開された候補カード一覧と評価入力欄を同じ画面に表示するよう変更した。
- デスクトップでは評価欄を右側に固定し、カードを見比べながら2点票・1点票・ナーフ対象票を選択できる。狭い画面ではカード一覧の下に評価欄を並べる。
- `npm.cmd test`: 17/17成功。ブラウザで公開カード一覧、投票先の自己除外、評価選択肢、console error/warn 0件を確認。

## 2026-07-12 居酒屋テーマへの全面刷新

- 木の卓、暖簾、赤い札、提灯色の灯り、お品書き風の紙面を共通UIとして採用し、開始・制作・公開・評価・結果の全フェーズに適用した。
- カード、調整札、評価票、提出済み印、ボタンを居酒屋の卓上小物として見える色・質感へ更新した。
- 開始・待機・ラウンド数などの案内文を「卓」「乾杯」「今夜の品評」基調に変更した。
- `npm.cmd test`: 17/17成功。開始画面をブラウザで目視確認、console error/warn 0件。

## 2026-07-12 一覧・品評の統合

- 全員の完成案がそろった時点で、一覧専用の公開フェーズを挟まず自動で品評フェーズへ移るようにした。最後に提出した参加者もすぐ評価できる。
- 品評画面は候補カード一覧と評価票を同一画面に統合した。
- 右側の評価票を2段構成（評価内容→選択欄）に変更し、狭い横幅でラベル文字が縦に崩れる問題を解消した。
- `npm.cmd test`: 17/17成功。ブラウザで自動遷移・統合画面・評価票の横組み・console error/warn 0件を確認。

## 2026-07-12 提出カードの表示整理

- 公開・品評・結果で使う提出カードから、攻撃／体力と調整カード名の表示を外した。
- 提出カードは担当者・コスト・カード名・能力テキスト（結果時は得点）に絞り、高さも短縮した。
- `npm.cmd test`: 17/17成功。

- 提出カードは調整カード名を非表示のまま、下角の攻撃／体力アイコンを復帰した。`npm.cmd test`: 17/17成功。

## 2026-07-12 レジェンド級提出カード枠

- 提出カードにだけ、金属の多重枠・紫の宝石・発光・角飾りを含むオリジナルのレジェンド級フレームを適用した。
- 原案・完成予定・調整札には適用せず、カードごとの役割を保った。
- `npm.cmd test`: 17/17成功。

## 2026-07-12 ヤバいカード投票

- 通常ラウンドの2点票・1点票を「ヤバいカード」への投票として整理し、ナーフ対象票と理由入力を削除した。
- 自分以外の全員から2点票を受けたカードは、ヤバすぎペナルティとして最終得点を0点にする。
- 結果ではヤバさ・ヤバすぎ：0点・最終得点を表示する。`npm.cmd test`: 17/17成功。

## 2026-07-12 4ラウンド固定

- 新規卓・ローカル/オンラインのゲームエンジン・サーバーを、全4ラウンド固定に統一した。
- ラウンド数の選択UIをなくし、最終看板カードのボーナスは3点に固定した。
- `npm.cmd test`: 17/17成功。
## 2026-07-12 交換フェーズの原案表示

- 交換フェーズを「左：原案カード / 右：交換する手札」の2カラムに再配置し、原案の能力・数値を常に確認できるようにした。
- 交換選択数（0〜2枚）と、0枚時の確定文言を明示した。幅980px以下では縦積みに切り替わる。
- `node --check src/ui.js`、`node --check server.mjs`、`npm.cmd test`: 17/17成功。
- ブラウザ自動確認は、環境のブラウザ一時セッション領域へのアクセスが利用上限で承認されず実施できなかった。
## 2026-07-12 数値カードの対象選択

- 数値カードに応じて数値候補を絞った。超軽量化は攻撃力・体力のみ、倍プッシュは能力文中の数字のみを表示する。
- 大型化は数値選択なしで、調整カードを選んだ時点でコスト+2・攻撃力/体力+3を完成予定へ即時反映する。
- 数値対象の絞り込みと大型化の即時適用をユニットテストに追加。`npm.cmd test`: 18/18成功。
## 2026-07-12 カードフレームの統一

- 原案・完成予定・通常調整カードを、提出カードと同じプリズム枠、名前帯、淡い虹色のカード面へ統一した。
- 原案と完成予定は緑コスト、青い剣の攻撃、赤い盾の体力を提出カードと共通化した。調整カードは情報量に合わせて小型フレームにした。
- `npm.cmd test`: 18/18成功。
## 2026-07-12 調整カードの札デザイン維持

- カードフレーム統一の対象から通常・ベーシック調整カードを外し、従来の卓上札デザインへ戻した。
- 原案・完成予定・提出カードのプリズムフレーム統一は維持する。`npm.cmd test`: 18/18成功。
## 2026-07-12 調整カード注記の自動処理

- a94・a101の「いずれから1つを選ぶ」を `choices` メタデータへ移し、調整フェーズでは選択肢ボタンから選んだ内容だけを完成カードへ反映するようにした。
- b5の「最終コスト+2」は `auto: enhance_cost_plus_2` として処理し、完成予定の最終コストから【エンハンス_X】を自動計算する。
- 調整カード本文の `※` 注記は0件。`npm.cmd test`: 19/19成功。ローカルサーバーを再起動して反映した。
## 2026-07-12 調整フェーズ内の引き直し

- 交換フェーズを廃止し、開始後は直接調整フェーズへ入るようにした。
- 調整フェーズ中は「引き直しモード」で不要な通常札を1枚ずつ選び、各ラウンド最大2回まで引き直せる。
- 通常調整カードの使用上限を撤廃し、手札にある通常札はすべて完成予定へ適用できる。
- 旧交換フェーズの保存済み卓は、サーバー復元時に調整フェーズへ移行する。
- `npm.cmd test`: 19/19成功。ローカルサーバーを再起動した。
## 2026-07-12 新規文章ブロックへの即時配置

- 効果・条件カードを選択中に「＋ 新規文章ブロック」を押すと、新しいブロックを作成して同じ操作でそのカードのテキストを配置するようにした。
- 選択肢付きカードは選択済みの候補を、条件カードは先頭へ反映する。
- `npm.cmd test`: 20/20成功。
## 2026-07-12 新規文章ブロックの重複追加修正

- 旧ハンドラが残り、新規文章ブロックが二重に追加される不具合を修正した。
- 選択中カードのテキストを入れた新規ブロックだけが1つ追加される。`npm.cmd test`: 20/20成功。
## 2026-07-12 Renderスリープ回避

- `GET /api/keepalive` を追加し、`Cache-Control: no-store` で軽量な生存確認を返すようにした。
- 会議参加中はクライアントが開始直後と以後5分ごとにこのAPIを呼び、最終結果または退出時は停止する。
- `npm.cmd test`: 20/20成功。ローカルAPIは200応答、Playwrightで卓作成後の画面・状態・console error 0件を確認した。

## 2026-07-12 提出中表示

- 完成予定の提出ボタンを押すと、即時に「完成予定を提出中…」へ切り替え、無効化と受付メッセージを表示するようにした。
- 通信完了前の重複提出を防ぎ、失敗時は再提出できる。
- `npm.cmd test`: 20/20成功。送信を1.2秒遅延させた実ブラウザ確認で、提出中の文言・無効化・メッセージとconsole error 0件を確認した。
