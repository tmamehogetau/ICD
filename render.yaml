services:
  - type: web
    name: izakaya-card-meeting
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install && npm run cards:build
    startCommand: npm start
    healthCheckPath: /
    branch: main
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22.12.0